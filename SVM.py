import numpy as np
inputS = "this is e-types favorite song"
inputT = "this, this is the way"

# Definition 1: 
# Sigma i a finite alphabet
# Strings are finite sequence of characters, including empty seq.
# |s| denotes the lenght of string s
#  s[i:j] is the substring s_i , ... , s_j
# u is a subsequence of s if there exists incices i = (i_1, ... ,i_|u|) with 1<= i_1 < ... < i_|u|< |s|
# such that u_j = s_{i_j} or u=s[i] for short
# the length l(i) if the subsequence in s is i_[u] - i_1 + 1
lambda = 0.5
K = []
k = i = 2
def K_prim(s, t, lambda, i):
    if (i = 0)
        return 1
    if (len(s) < i or len(t) < i):
        return 0


def K(s,t):
    if (len(s) < i or len(t) < i):
        return 0



for val in K:
    K_prim(s, t, lambda, )

